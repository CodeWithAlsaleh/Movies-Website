# 🍿 watchMovies — Movie Library Manager

Built with React — a dynamic movie search and management dashboard to discover films, rate them, and track your watch list. Lightweight, component-based, and highly interactive.

## 🚀 Features

- **Real-time Movie Search** — Fetches movie data from the OMDb API as you type with debouncing logic.
- **Detailed Movie Insights** — View plot, actors, director, and IMDb ratings for any selected film.
- **Interactive Rating System** — Rate movies using a custom Star component before adding them to your list.
- **Watch List Tracking** — Keep track of the movies you've watched and view summary statistics (avg rating, runtime).
- **Persistent Storage** — Uses Local Storage to ensure your watched list is saved even after refreshing the page.
- **Responsive Layout** — A clean, dark-themed UI that adapts to different screen sizes.
- **Custom Hooks** — Built with modular custom hooks like `useMovies` and `useLocalStorageState` for clean, reusable logic.

## 🛠️ Technologies Used

- **React.js** (Functional Components & Hooks)
- **OMDb API** (External Data Fetching)
- **CSS3** (Modern styling & Flexbox)
- **JavaScript** (ES6 modules & LocalStorage)
